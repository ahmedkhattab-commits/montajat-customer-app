/// Pusher integration is intentionally disabled until environment-specific
/// credentials are supplied through secure configuration.
abstract final class PusherService {
  static const bool isConfigured = false;
}
