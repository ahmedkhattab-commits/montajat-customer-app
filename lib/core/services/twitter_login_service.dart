/// Twitter authentication is intentionally disabled until its credentials
/// are supplied through secure platform configuration.
abstract final class TwitterLoginService {
  static const bool isConfigured = false;
}
